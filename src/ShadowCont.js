import styled from 'styled-components'
import Container from 'react-bootstrap/Container'

const ShadowCont = styled(Container)``

ShadowCont.defaultProps = Container.defaultProps

export default ShadowCont
